# Apple Build Secrets Generic Bundle

This export is for model stacks that do not use Codex or Claude skill folders directly.

Use it like this:

1. Load `knowledge/macos/00_INDEX.md`
2. Load one primary domain doc
3. Optionally load `prompts/APPLE_APP_DEVELOPER_PROMPT.md`
4. Do not load the entire library unless you are auditing or planning broadly

The goal is to keep context small while still giving a model enough platform-specific structure to reason well about macOS work.

